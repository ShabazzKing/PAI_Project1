extends ../layout

block content
    h1= title
    if data.length > 0
        table
            tr
                th Nr
                th Nazwa
                th Stan
            each val, index in data
                tr
                    td= index + 1
                    td: a(href='/auctions-finished/'+val.auction_id)= val.auction_title
                    td Przetarg zakończony
    else
        p Brak zakończonych przetargów
